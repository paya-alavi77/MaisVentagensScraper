services:
  scraper:
    build: .
    container_name: maisvantagens_scraper
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=America/Sao_Paulo
    volumes:
      - ./logs:/app/logs
      - ./cookies.json:/app/cookies.json
